package android.content

/**
 * Compile-time stub for android.content.Context to ensure exact binary compatibility
 * with CloudStream's Plugin.load(Context) method signature on Android ART/Dalvik.
 */
open class Context
